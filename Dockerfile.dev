# 开发环境 Dockerfile（快速启动，支持热重载）
FROM node:18-alpine AS development

WORKDIR /app

# 只复制依赖文件
COPY package.json package-lock.json* ./

# 安装依赖（包括开发依赖）
RUN npm ci

# 复制其他文件
COPY . .

# 暴露端口
EXPOSE 3000

# 开发模式启动
CMD ["npm", "run", "dev"]

